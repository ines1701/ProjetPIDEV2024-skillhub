
{% block body %}

{{ form_start(form, {'attr': {'class': 'my-form-class'}}) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

{% endblock %}

