{% block content %}
{{ form_start(form) }}
    {{ form_widget(form, {'attr': {'novalidate': 'novalidate', 'class': 'my-form-class'}}) }}
    <div style="text-align: center;">
    <button class="btn">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}
{% endblock %}