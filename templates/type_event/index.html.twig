{% extends 'base.html.twig' %}

{% block title %}TypeEvent {% endblock %}

{% block content %}
<br></br><br></br>
    <h1>TypeEvent </h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Label</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_event in type_events %}
            <tr>
                <td>{{ type_event.id }}</td>
                <td>{{ type_event.label }}</td>
                <td>
                  
                     {{ include('type_event/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_event_new') }}">Create new</a>
{% endblock %}
